# Standardized Kubernetes Service for web
# Generated by container standardization script
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: web
    part-of: maily
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: web
