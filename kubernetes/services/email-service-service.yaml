# Standardized Kubernetes Service for email-service
# Generated by container standardization script
apiVersion: v1
kind: Service
metadata:
  name: email-service
  labels:
    app: email-service
    part-of: maily
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: email-service
