# Standardized Kubernetes Service for workers
# Generated by container standardization script
apiVersion: v1
kind: Service
metadata:
  name: workers
  labels:
    app: workers
    part-of: maily
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: workers
