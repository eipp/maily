{
  "name": "@maily/[app-name]",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "[app-specific-dev-command]",
    "build": "[app-specific-build-command]",
    "start": "[app-specific-start-command]",
    "lint": "eslint .",
    "test": "[app-specific-test-command]",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@maily/ui": "*",
    "@maily/config": "*",
    "@maily/utils": "*"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "eslint": "^8.38.0",
    "eslint-config-maily": "*",
    "typescript": "^5.0.4"
  }
}
